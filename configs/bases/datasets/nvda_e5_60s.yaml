# axis: datasets
# NVDA E5 time-based 60s bins — 98 features, hybrid normalization
# Used by: E5 60s family (3 configs), E6 calibrated conviction
#
# Ownership (per §3.4): data.data_dir, feature_count, sources, normalization,
#                        sequence.{window_size, stride}, model.input_size
#                        (auto-derived — locked to feature_count)
_partial: true

data:
  # v3p0 baseline (Phase O Cycle 1, 2026-05-04): re-extracted with v0.2.1
  # binaries; schema_version=3.0; idx 97=0.0 RESERVED; +21% data on 164/233
  # days from session-Clear handling fix (B.2). The bare
  # ../data/exports/e5_timebased_60s/ is POLLUTED (mixed schema {2.2,3.0} +
  # mixed commit {b5e746d,c5e9d64} — NOT uniformly 2.2; verified 2026-05-29) and
  # FAILS both validate_export_contract (validation.py:115-120) and the dir-level
  # validate_export_dir (8 violations). Use _v3p0 only.
  data_dir: "../data/exports/e5_timebased_60s_v3p0"
  feature_count: 98
  sequence:
    window_size: 20
    stride: 1
  normalization:
    strategy: hybrid
    eps: 1.0e-8
    clip_value: 10.0
    exclude_features: [93]

model:
  input_size: 98   # locked to data.feature_count per T13
