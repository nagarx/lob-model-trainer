# axis: datasets
# NVDA E5 time-based 60s bins — 98 features, hybrid normalization
# Used by: E5 60s family (3 configs), E6 calibrated conviction
#
# Ownership (per §3.4): data.data_dir, feature_count, sources, normalization,
#                        sequence.{window_size, stride}, model.input_size
#                        (auto-derived — locked to feature_count)
_partial: true

data:
  data_dir: "../data/exports/e5_timebased_60s"
  feature_count: 98
  sequence:
    window_size: 20
    stride: 1
  normalization:
    strategy: hybrid
    eps: 1.0e-8
    clip_value: 10.0
    exclude_features: [93]

model:
  input_size: 98   # locked to data.feature_count per T13
